# Jync v1.3.1

本版主题是**AI 起草提速与界面观感**：候选 SQL 流式进编辑器、边生成边可见；模型只回转换后的 CREATE 语句，输出即所得；同一对象重复起草走 10 分钟缓存直接返回。复审页编辑器与对比框新增 SQL 高亮，全站换细滚动条。另修复一处同步稳定性问题：运行中清空目标库不再误报乐观锁错误。

## 本次更新

### AI 起草提速

- **流式输出**：复审页「用 AI 起草」改走流式（SSE），候选边生成边渲染进编辑器，不再等超时才一次性看到全文
- **缓冲型网关兼容**：整段生成完才返回的自建/中转端点自动回退为全文一次渲染；流式超时语义改为「相邻两包间隔超过设定秒数才断开」，首包等待不受该超时限制，慢模型不再被误判超时
- **块格式自动识别**：端点实际返回的块格式与配置的协议（OpenAI / Anthropic）不一致时按行自动识别，仍能正常流式读取
- **10 分钟起草缓存**：同一对象、同一供应商、源定义未变时重复点击立即返回，提示中标注（缓存命中）；源定义或模型变了自动失效
- **输出更直接**：模型只回复转换后的 CREATE 语句（无说明文字、无 JSON 包装），编辑器原样呈现；起草完成后页面给一条提醒 —— AI 输出仅为候选，保存为覆盖前请先在目标库人工自测

### 界面

- **SQL 高亮**：复审页的覆盖编辑器与「原始定义 / 自动转换」对比框对关键字、字符串、数字、注释分区着色，风格参照 DBeaver
- **细滚动条**：全站滚动条改细、彩虹渐变、两端圆头，深浅色主题都适配

### 同步稳定性

- **修复乐观锁误报**：运行中清空目标库/目标表时，行数审计清游标与同轮推进游标相撞，变更日志报 `StaleObjectStateException` 红色错误（不影响数据，但吓人且下轮多付一次全量）。现在审计清游标后沿用保存返回的实例继续；进度行万一被并发写过（如手工重置），推进游标时自动重读重试一次
- 新增契约测试锁定游标交接行为；单元测试增至 419 个

### 内部清理（行为不变）

- 移除 22 项 Java 死代码（无调用方法、只写字段、无调用派生查询）与 12 项前端死规则 / 死资源；单元测试增至 417 个

## 升级

换 jar、重启即可，配置无需改动，元数据库与已存加密口令不受影响。元数据库中的 `captured_at`、`task_name` 两列本版起不再使用，列会留着（废弃但无害），无需手工清理。

## 完整文档

[README（中文）](https://github.com/vfaner/jync/blob/main/README.md) · [README (English)](https://github.com/vfaner/jync/blob/main/README_EN.md)
